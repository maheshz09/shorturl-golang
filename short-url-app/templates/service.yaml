apiVersion: v1
kind: Service
metadata:
  name: {{ include "short-url-app.fullname" . }}
  labels:
    {{- include "short-url-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "short-url-app.selectorLabels" . | nindent 4 }}
